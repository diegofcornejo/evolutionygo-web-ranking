---
interface Props {
	rating: number;
}
const { rating } = Astro.props;
---

<div class="rating">
  {Array.from({ length: rating }).map((_, index) => (
    <input type="radio" name="rating-2" class="mask mask-star-2 bg-orange-400 pointer-events-none"/>
  ))}
</div>